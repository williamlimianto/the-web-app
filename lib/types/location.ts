type CommonLocationFields = {
  id: number;
  value: string;
};

// NOTE: Feel free to extend using "&" if needed.
export type Country = CommonLocationFields;

// NOTE: Feel free to extend using "&" if needed.
export type State = CommonLocationFields;
